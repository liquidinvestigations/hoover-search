def split_pdf_file(streaming_content):
    yield b'1'
    # for chunk in streaming_content:
    #     yield chunk


def get_pdf_info(streaming_content):
    yield b'2'
    # for chunk in streaming_content:
    #     yield chunk
